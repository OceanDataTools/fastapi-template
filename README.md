Initial setup/install:

Currently uses Poetry for Python package management... probably will change to pip to remove the dependency
```
pip install poetry
```

from OpenRVDAS Install base (e.g /opt/openrvdas)
```
./web_backend/setup.sh
```

Start backend:
from OpenRVDAS Install base (e.g /opt/openrvdas)
```
./web_backend/entrypoint.sh
```